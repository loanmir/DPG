# Empty package marker so the ``categorical`` grouping scripts can be
# imported by run_all_categorical_experiments.py and the interactive
# website (interactive_runs/runner.py). Kept empty on purpose; the
# scripts retain their CLI entry points.
