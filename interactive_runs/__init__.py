# Marks ``interactive_runs`` as a Python package so that
# ``from interactive_runs import …`` resolves both when running
# ``python -m interactive_runs.app`` and when the runner is invoked as
# part of the broader pipeline.
